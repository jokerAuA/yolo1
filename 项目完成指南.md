# 无人机小目标检测毕设完成指南

## 整体流程

```
第1周  环境 + 数据集
第2周  基线训练
第3-4周 模型改进 + 消融实验
第5周  导出 + 轻量化
第6周  界面开发
第7-9周 论文撰写
第10周 答辩准备
```

---

## 第一步：安装环境

```bash
pip install ultralytics sahi gradio onnxruntime pandas pillow
```

打开 `01_data_preparation.ipynb`，运行第一个单元格验证安装成功。

---

## 第二步：下载 VisDrone 数据集

**推荐方式（Kaggle，最快）：**

1. 搜索 `VisDrone2019` 找到数据集页面
2. 下载后解压到项目 `data/VisDrone/` 目录

解压后目录结构应为：

```
data/VisDrone/
├── VisDrone2019-DET-train/
│   ├── images/        ← 约6471张图
│   └── annotations/   ← 对应txt标注
├── VisDrone2019-DET-val/
│   ├── images/
│   └── annotations/
└── VisDrone2019-DET-test-dev/
    ├── images/
    └── annotations/
```

**格式转换：** 运行 `01_data_preparation.ipynb` 的转换单元格，会在 `data/VisDrone/labels/` 下生成 YOLO 格式标注。

---

## 第三步：修改数据集配置

打开 `configs/visdrone.yaml`，确认路径正确：

```yaml
path: ../data/VisDrone
train: images/train
val: images/val
test: images/test
```

同时把转换后的图片软链接或复制到对应 `images/train`、`images/val`、`images/test` 目录。

---

## 第四步：训练基线模型

打开 `02_train_baseline.ipynb`，依次运行：

1. 训练单元格（约需 2-4 小时，GPU 环境）
2. 评估单元格，记录以下数据（**这是论文"改进前"数据**）：
   - mAP@0.5
   - mAP@0.5:0.95
   - FPS

训练结果自动保存到 `results/baseline/`。

> **没有 GPU？** 用 Google Colab：上传整个项目文件夹，选择 T4 GPU 运行时，免费可用。

---

## 第五步：模型改进（核心）

打开 `03_improved_model.ipynb`，按顺序做三组实验：

### 实验一：SAHI 切片推理

- 不需要重新训练，直接用基线权重
- 运行 SAHI 单元格，对比检测到的小目标数量
- 记录检测结果图，放入论文第五章

### 实验二：CBAM 注意力机制

- 运行 CBAM 训练单元格（训练配置与基线相同）
- 训练完成后评估，记录 mAP 数据

### 消融实验表格

运行最后的汇总单元格，自动生成对比表：

| 模型 | mAP@0.5 | mAP@0.5:0.95 |
|------|---------|--------------|
| Baseline | - | - |
| Baseline + SAHI | - | - |
| CBAM | - | - |

把这张表复制进论文第五章，这就是消融实验。

---

## 第六步：导出与轻量化

打开 `04_export_deploy.ipynb`：

1. 导出 ONNX（FP16 量化）
2. 对比 PyTorch vs ONNX Runtime 的 FPS

论文中写：*"通过 FP16 量化导出 ONNX 模型，推理速度提升 X 倍，适配边缘计算平台部署需求。"*

---

## 第七步：答辩演示界面

打开 `05_demo_ui.ipynb`，运行唯一的单元格，浏览器自动打开 Gradio 界面。

- 支持上传图片，实时显示检测框
- 答辩时直接在浏览器演示，无需额外操作

---

## 第八步：论文撰写

| 章节 | 内容来源 |
|------|---------|
| 第1章 绪论 | 开题报告扩展 |
| 第2章 理论基础 | YOLOv11 原理、CBAM 原理 |
| 第3章 数据集 | VisDrone 介绍 + 转换过程截图 |
| 第4章 改进方法 | SAHI 原理图 + CBAM 结构图 |
| 第5章 实验分析 | 消融实验表 + Loss/mAP 曲线图 |
| 第6章 系统设计 | Gradio 界面截图 |
| 第7章 总结 | 工作总结 + 不足与展望 |

**图表来源：**
- Loss/mAP 曲线：`results/baseline/results.png`（Ultralytics 自动生成）
- 检测结果对比图：SAHI 单元格输出的 `results/sahi/` 目录

---

## 常见问题

**Q：显存不够，batch=16 报错？**
改为 `batch=8` 或 `batch=4`，mAP 影响不大。

**Q：没有 Jetson 开发板怎么写论文？**
写 *"已完成 FP16 量化，在 PC 上通过 ONNX Runtime 验证推理加速，模型文件见附件"*，完全没问题。

**Q：mAP 提升很小怎么办？**
哪怕只提升 1-2 个点，只要消融实验表格完整，论文就能通过。数量比幅度重要。
